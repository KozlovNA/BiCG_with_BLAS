# BiCG_with_BLAS

## Quick start

C++ Library can be build and installed using the following commands:
```
mkdir output
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .
./main
```